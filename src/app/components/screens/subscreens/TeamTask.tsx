import React from "react";

const TeamTask: React.FC = () => {
   return (
       <div style={{ padding: "20px", textAlign: "center" }}>
           <h1>Team Task</h1>
           <p>Assign tasks to team members for social media campaigns or other projects.Include task statuses (To Do, In Progress, Done) and due dates.Use push notifications to keep the team updated.</p>
       </div>
   );
};

export default TeamTask;
